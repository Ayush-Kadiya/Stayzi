<% layout("/layouts/boilerplate") %>
<div class="row mt-5">
    <div class="alert alert-danger mt-3 col-10 " role="alert">
        <h4 class="alert-heading">Error!</h4>
        <p><%= err.message %></p>
        <p class="mb-0">An unexpected error occurred while processing your request.</p>
        <hr>
        <p class="mb-0">Please try again later or contact support.</p>
    </div>
</div>